---
name: prd-to-dev-spec
description: "Use when a confirmed PRD exists and user needs a development-ready spec, engineering fact check, gap report, or go/no-go coding decision. Triggers: PRD转开发就绪规格、生成开发规格、技术规格、开工评审、开发前检查、判断能否开始编码、generate dev-ready spec from PRD."
---

# PRD to Dev Spec — 开发就绪规格与开工门禁

## 用途

输入已确认的 PRD（可选叠加原型、代码库、接口契约文档），对每个页面、状态、用户操作执行工程事实核对。**事实齐则放行，事实缺则停在缺口报告。**

**核心原则：本技能产出的是门禁裁决，不是技术方案。它回答"能不能开始编码"，不回答"怎么实现"。**

### 与上下游的分工（三层架构）

| 层 | 文档 | 职责 | 产出技能 |
|----|------|------|----------|
| 产品层 | PRD | 用户可见数据 + 业务规则 | proto-to-prd |
| 规格层（本层） | 开发就绪规格 | 工程事实核对 + 开工裁决 | prd-to-dev-spec |
| 契约层 | 接口契约 | 字段、端点、请求/响应结构、错误码 | 独立维护 |

本技能写"语义层"（对象、范围、投影、幂等规则），**不定义 API 结构**（归契约层），**不新增产品规则**（归 PRD，经回流机制）。

### 输入分级

| 模式 | 输入 | 产出 |
|------|------|------|
| 完整模式 | 已确认 PRD + 定稿原型 + 代码库/接口契约 | 正式开工裁决 |
| 半模式 | 已确认 PRD + 接口契约文档（无代码） | 裁决；证据标"基于契约文档" |
| 预检模式 | 仅已确认 PRD | **PRD 预检报告**：R-XXX 照常回流改 PRD；T-XXX 全标"待实现核对"；G4 固定不通过 |

预检模式的价值：在代码动工前提前暴露 R-XXX 和工程维度缺口，越早发现越便宜。

## 适用场景

- 用户提供已确认 PRD，要求生成开发就绪规格、技术规格、开工评审
- 用户问"这个 PRD 可以开始开发了吗"
- 缺口补齐后要求重检、关闭缺口、更新文档状态
- PRD 升版后要求同步核对规格

## 不适用场景

- PRD 未确认（先退回 proto-to-prd 完成批次确认清零）
- 写技术方案/架构设计（本技能只做事实核对与裁决）
- 定义 API 结构、字段、错误码（归接口契约文档）
- 修改产品规则（只能通过 R-XXX 回流，由用户决策）

## 执行流程

### Step 1：前置检查

1. **验证 PRD 已确认**：
   - 待确认事项已清零
   - 决策与确认记录已沉淀（Q-001 起编号）
   - PRD 存在业务状态流转时，已提供状态主表或等价的唯一状态清单；正式状态名称在状态机、页面规则、操作映射、固定文案和验收标准中一致
   - 若 PRD 未确认 → **停止**，告知用户先完成 PRD 确认流程
2. **确定输入模式**：完整 / 半 / 预检（缺什么降什么级）
3. **消费上游预留**：读取 PRD 的「开发就绪规格待补充」章节（D 项）与排除表的「后续归属」列，作为初始采集清单

### Step 2：核对范围确认

向用户展示并确认：

1. **核对对象清单**：继承 PRD 页面清单编号（G/P/S/M），列出本次核对的页面、业务对象、关键用户操作。**核对对象编号必须沿用 PRD 编号，禁止自造分组编号**（功能分组仅作组织方式，不产生新编号）
2. **显式排除表**：本次不核对的对象逐项列出并说明原因（如"纯前端展示，无后端契约需求"）。与 PRD 排除表同理——排除必须写明原因，避免读者分不清"漏了"还是"有意排除"
3. **命中的触发维度**：触发 4 中哪些命中（含判断依据，如"存在编号生成场景 → 并发与唯一性命中"）
4. **输入模式**及其限制（预检模式提前声明 G4 将不通过）

用户确认后进入逐维度核对。

### Step 3：逐维度核对（核心）

#### 维度清单

**必查 8（每个核对对象都必须过）：**

| 维度 | 核对问题 |
|------|----------|
| 业务对象与数据模型 | 该操作涉及哪些实体？字段、关系、数据归属是否已有合同？ |
| 读写操作映射 | 每个用户操作映射到什么命令/查询？读哪些、写哪些？ |
| 权限与服务端数据范围 | 服务端是否以身份限制数据范围？越权返回什么？前端筛选不得作为权限边界 |
| 状态投影 | 内部状态如何映射为客户可见状态？不得直接复用内部阶段名 |
| 刷新与一致性 | 刷新、重进、返回后数据从哪取？缓存何时失效？ |
| 幂等与重复提交 | 重复点击、重复提交的防护？提交成功后按钮/表单状态？ |
| 失败恢复与错误分类 | 失败分几类？哪些可重试、哪些阻断？失败后页面状态？ |
| 外部依赖与数据源 | 依赖哪些外部服务/枚举数据？超时、降级策略？ |

**触发 4（命中条件才核对）：**

| 维度 | 触发条件 |
|------|----------|
| 会话与本地数据生命周期 | 存在本地状态、登录态依赖、登出清理、跨设备行为时 |
| 并发与唯一性 | 存在编号生成、限量资源、名额类操作时 |
| 审计与留存 | 合规类产品、需要操作留痕/时间戳留存时 |
| 运行时性能与容量 | 大列表、文件上传、大数据量场景时 |

#### 核对矩阵与三态结论

对每个核对对象 × 每个适用维度，产出三态之一：

| 结论 | 含义 | 要求 |
|------|------|------|
| **已有合同** | 工程事实已存在且可验证 | **必须附来源**（代码路径、契约文档章节、类型定义）；预检模式不允许出现此态 |
| **T-XXX** | 契约缺口：产品规则已明确，缺工程合同 | 登记到缺口表，含影响范围 |
| **R-XXX** | 规则回流项：PRD 没定、自相矛盾、或技术上不可行 | 登记到回流表，引用 PRD 原文 |

**禁止**出现"假定有""应该有""大概率已有"等无证据结论。

#### 缺口登记规范

- **T-XXX（契约缺口）**：编号、缺口描述、影响范围（页面编号列表）、产品规则状态（已明确/需回流）、处理方式、**关闭条件**（定义"什么算补齐"的可判定 DoD，如"契约文档含 X 并通过评审"——重检时按此判定可否关闭，不允许"感觉补齐了"就关闭）、状态（阻塞/待实现核对/已关闭/排除）
- **R-XXX（规则回流项）**：编号、PRD 原文引用、问题类型（缺失/矛盾/不可行）、建议方案、状态（待决策/已回流）
- R-XXX 不单独打断用户，**攒成批次**在 Step 4 后统一提交决策

### Step 4：门禁裁决

对照六项门禁逐项判定（详见模板第 1 章）。任一不通过 → 缺口报告态；全部通过 → 可开发态。

### Step 5：R-XXX 批次确认循环（回流机制）

1. **提交决策**：将所有 R-XXX 一次性提交用户，附 PRD 原文引用和建议方案
2. **用户拍板**：逐条回复结论
3. **双文档同步更新**（同一会话内执行）：
   - PRD 升版本，变更记录登记「触发来源：开发就绪规格核对 R-XXX」
   - 本文档同步引用新版 PRD，受影响维度的结论按新规则重新核对
4. **衍生问题检查**：新规则是否引出新缺口/新回流？
   - 有 → 新增编号，回到步骤 1
   - 无 → 回流完成
5. **清零沉淀**：R-XXX 全部回流后，结论沉淀到「决策与确认记录」章节

**规则回流项不允许规格文档直接改 PRD**——修改权必须留在产品确认流程。

### Step 6：输出文档

按模板生成本文档（见下方模板），保存到用户工作目录，命名 `DEV-SPEC-[功能名称].md`。

### Step 7：重检协议（变更应对）

**本协议的立场：文档没有"完成"的那一天，只有"当前真实"的每一天。**

| 变更事件 | 重检范围 |
|----------|----------|
| T-XXX 关闭 | 验证「关闭条件」满足后，按「影响范围」涉及的页面 × 维度重核 |
| R-XXX 回流（PRD 升版） | 新规则影响的页面 × 维度；核对矩阵中结论基于旧版的部分 |
| 代码/契约演进 | 下次核对时全量重验"已有合同"的证据是否仍存在 |
| 任何重检发现新问题 | 正常登记新 T-XXX / R-XXX，文档状态退回阻塞态 |

三条铁律：
1. **影响范围登记**：每个缺口必须登记影响范围，关闭时只重检受影响范围
2. **结论带版本**：每个"已有合同"结论记录证据锚点（PRD 版本 + 代码/契约位置）；过期结论不得蒙混在"已通过"中
3. **门禁只认当下**：不是"曾经通过"，是"当前通过"；重检后该红就红

### Step 8：一致性自检

生成或重检后，执行以下自检（不通过则修正后重检）：

1. **上游就绪**：PRD 是否为已确认版？文档信息是否记录了 PRD 版本锚点？
2. **维度覆盖与显式排除**：每个核对对象是否都过了必查 8 + 命中的触发 4？核对矩阵无空格？未核对对象是否全部在显式排除表中登记原因？
3. **证据真实**：每条"已有合同"是否都指向可验证位置？预检模式是否全部标"待实现核对"？
4. **上游消费**：PRD 的 D 项和排除表「后续归属」是否全部被 T/R 覆盖或说明？
5. **影响范围与 DoD**：每个 T-XXX 是否登记影响范围与关闭条件？
6. **边界纯净**：是否新增了产品规则？是否越界定义了 API 结构（字段、端点、错误码）？是否编造实现事实？
7. **回链完整**：验收基线是否每条回链 PRD 验收标准？门禁结论是否与缺口表状态一致？
8. **回流完整**：R-XXX 是否全部已回流（PRD 已更新且本文档已同步）？未决策的是否保留在回流表？
9. **状态 SSOT 一致性**：PRD 存在状态流转时，每个正式状态是否具有唯一文档级状态 ID 或等价唯一锚点？状态机、状态变更表、卡片变体、操作映射、固定文案和验收标准是否使用同一正式名称？发现未登记别名、分类冒充状态、一状态多名称或孤立状态时，必须登记 R-XXX，G1 不通过，不得将别名自行解释成同一状态后继续放行。

自检报告格式：

```markdown
## 自检报告
| 检查项 | 结果 | 详情 |
|--------|------|------|
| 上游就绪 | 通过/需修正 | ... |
| 维度覆盖与显式排除 | 通过/需修正 | ... |
| 证据真实 | 通过/需修正 | ... |
| 上游消费 | 通过/需修正 | ... |
| 影响范围与 DoD | 通过/需修正 | ... |
| 边界纯净 | 通过/需修正 | ... |
| 回链完整 | 通过/需修正 | ... |
| 回流完整 | 通过/需修正 | ... |
| 状态 SSOT 一致性 | 通过/需修正 | [状态 ID/唯一锚点、正式名称与跨章节引用是否一致] |
```

## 文档模板

```markdown
# 开发就绪规格：[功能/页面名称]

> 本文档将已确认的 PRD 体验与工程事实逐项核对。不新增产品规则、API、字段或路由；
> 工程事实缺失时停在缺口报告，不得据此开始编码。

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v0.1 |
| 文档状态 | 缺口报告（阻塞，尚不可开发）/ PRD 预检报告 / 可开发 |
| 基于 PRD | `PRD-[名称].md` vX.Y（已确认） |
| 输入模式 | 完整 / 半 / 预检 |
| 原型来源 | [定稿原型引用，如有] |
| 覆盖端 | [端] |

## 1. 开工结论

| 门禁 | 检查内容 | 结论 | 说明 / 证据 |
|------|----------|------|-------------|
| G1 上游就绪 | PRD 为已确认版（待确认清零、决策已沉淀）；有原型时原型已定稿 | 通过/不通过 | ... |
| G2 维度核对完成 | 每个页面/状态/操作都过完适用维度，无未核对组合 | 通过/不通过 | ... |
| G3 缺口闭环 | 所有 T-XXX 已关闭或明确排除；R-XXX 已回流完成（PRD 已更新且本文档已同步） | 通过/不通过 | ... |
| G4 实现核对完成 | 所有"已有合同"指向真实存在的代码/契约文档；预检模式固定不通过 | 通过/不通过 | ... |
| G5 验收可观察 | 每条验收基线为可观察结果，回链 PRD 验收标准 | 通过/不通过 | ... |
| G6 边界纯净 | 未新增产品规则、未越界定义 API 结构、未编造实现事实 | 通过/不通过 | ... |

**结论：[可开始编码 / 不可开始编码（原因：G-X 不通过，见 T-XXX）]**

## 2. 核对范围

核对对象沿用 PRD 编号（G/P/S/M），列出本次核对的页面、业务对象、关键用户操作；
命中的触发 4 维度及判断依据。

**显式排除表**（本次不核对的对象，逐项说明原因）

| 排除对象 | 排除原因 |
|----------|----------|
| [编号+名称] | [如：纯前端展示，无后端契约需求 / 属于其他 Flow 范围] |

## 3. 工程事实核对（主表）

### [P-01] [页面名]

**核对对象**：[用户操作 / 业务对象 / 状态]

| 维度 | 产品规则（PRD 引用） | 工程事实 | 结论 |
|------|---------------------|----------|------|
| 业务对象与数据模型 | PRD §X.Y：[规则] | [已有：...（证据：`路径`）/ 缺：见 T-001] | 已有合同 / T-001 / R-002 |
| 读写操作映射 | ... | ... | ... |
| （必查 8 逐项） | ... | ... | ... |
| （命中的触发维度） | ... | ... | ... |

（每个核对对象一节，同一事实只在本表登记一次，缺口表只做编号引用）

## 4. 缺口登记

### 4.1 契约缺口（T-XXX）

| 编号 | 缺口描述 | 影响范围 | 产品规则 | 处理方式 | 关闭条件 | 状态 |
|------|----------|----------|----------|----------|----------|------|
| T-001 | [缺什么工程事实] | [页面编号列表] | 已明确（PRD §X.Y）/ 需回流（见 R-XXX） | [补契约/实现/排除] | [可判定的 DoD：什么算补齐] | 阻塞/待实现核对/已关闭/排除 |

### 4.2 规则回流项（R-XXX）

| 编号 | PRD 原文引用 | 问题类型 | 建议方案 | 状态 |
|------|--------------|----------|----------|------|
| R-001 | `PRD §X.Y：[原文]` | 缺失/矛盾/不可行 | [建议] | 待决策/已回流 |

## 5. 补齐顺序

### 5.1 缺口补齐顺序

1. [按依赖排序：先权限与数据范围，再状态投影，再写操作……逐项列出，标注对应 T-XXX]

### 5.2 先行开发范围（待用户决策）

> 门禁不通过不代表全部工作停摆。哪些开发工作可以先动工（如不依赖缺口契约的纯前端
> UI 层）由用户在决策中明确后填入本节；**规格不得自行填写或暗示放行范围**。
> 未决策时保留本占位说明。

## 6. 验收基线

> 分工声明：前端可见行为的验收以 PRD 各页面的 GIVEN-WHEN-THEN 场景为准；本章只覆盖
> 服务端可观察与工程维度的验收。开发完成需同时通过两套验收，不得以本章替代 PRD 验收。

| 基线 | 可观察结果（缺口关闭后必须可验证） | 回链 PRD | 状态 |
|------|----------------------------------|----------|------|
| [基线名] | [服务端可观察的行为，非愿望描述] | [PRD 验收标准场景编号 / R-XXX 决策] | 已可执行 / 依赖 R-XXX 决策 / 依赖 T-XXX 关闭 |

## 7. 决策与确认记录

| 编号 | 问题 | 确认结论 | 确认日期 | 状态 |
|------|------|----------|----------|------|
| R-001 | [原回流项] | [用户决策结论] | YYYY-MM-DD | 已回流 |

## 8. 变更记录

| 日期 | 版本 | 基于 PRD | 变更 |
|------|------|----------|------|
| YYYY-MM-DD | v0.1 | vX.Y | 初次核对（输入模式：...） |
```

**放行态与阻塞态的差异**：结构完全相同（原地升级，不换文档）。缺口全关闭后：第 1 章门禁全绿、结论改「可开始编码」；第 4 章缺口全部转「已关闭」；第 6 章验收基线成为开发完成的验收依据；文档状态字段改「可开发」。

## 关键约束

### 必须做到

- **结论三态**：每格核对结论只能是「已有合同（附来源）/ T-XXX / R-XXX」，禁止无证据的"假定有"
- **版本锚定**：文档信息记录基于的 PRD 版本；PRD 升版时同步更新并重检受影响范围
- **状态名称不猜测**：PRD 出现同义、缩写、泛化或组合状态名时，不得自行合并；登记 R-XXX 回流产品层，以状态主表确认后的正式名称为准
- **影响范围登记**：每个 T-XXX 登记影响的页面编号，关闭时按范围精准重检
- **门禁只认当下**：任何重检发现新问题，状态退回阻塞态，哪怕昨天全绿
- **回流走流程**：PRD 修改只能经 R-XXX 批次确认 → 用户拍板 → 双文档同步更新，不得直接改
- **降级如实标注**：预检模式所有结论标"待实现核对"，G4 固定不通过，不得伪装成正式裁决
- **决策沉淀**：R-XXX 清零后生成决策与确认记录，确认过程不允许丢弃
- **缺口有 DoD**：每个 T-XXX 必须登记关闭条件（可判定的完成定义），重检按关闭条件判定，不允许"感觉补齐了"就关闭
- **先行范围用户定**：先行开发范围只能由用户决策明确后填入，规格不得自行填写或暗示放行
- **前端不等于权限**：权限边界必须在服务端，前端筛选不能作为权限结论的依据
- **mock 不等于合同**：示例数据、本地状态、mock 下载不得登记为「已有合同」

### 禁止行为

- **编造事实**：不得把"应该会有""大概有"写成已有合同
- **越权改 PRD**：不得在规格流程中直接修改产品规则
- **越界写契约**：不得在本文档定义 API 字段、端点、错误码结构（引用契约文档可以）
- **替产品决策**：不得自行替用户拍板 R-XXX 的建议方案之外的内容
- **缺口误报为可开发**：门禁不通过时不得给出"可以先做核心部分"之类的放行变体；部分放行必须由用户在 R-XXX 决策中明确

## 与其他技能的协作

| 场景 | 推荐技能 | 说明 |
|------|----------|--------|
| 上游：原型转 PRD | proto-to-prd | 本技能消费其页面清单编号、D 项、排除表 |
| PRD 需要补确认 | proto-to-prd 批次确认 | R-XXX 回流时的确认机制 |
| 下游：接口契约 | 独立维护 | 本技能登记 T-XXX 后由契约文档承接，证据回链 |
| 技术方案设计 | 直接对话 | 门禁通过后的实现设计不属本技能 |
```
